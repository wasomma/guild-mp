# Art pipeline: the hybrid plan

Agreed direction (owner, 2026-07-21): **characters stay procedural** (the paperdoll — class × hair × outfit × weapon × cape × aura — makes static sprite sheets combinatorially explosive, and gear-visible-on-sprite is core to the reward loop). **Non-combinatorial art gets generated externally** via specialist pixel-art models, imported as assets.

## What gets generated vs. drawn in code

| Asset | Source | Why |
|---|---|---|
| Adventurer sprites, gear, weapons, capes, pets | Procedural (code) | Paperdoll combinatorics; rarity legibility |
| Zone backgrounds (4 zones × bg/mid layers) | Generated (PixelLab / Retro Diffusion) | Fixed count, biggest visual win |
| Enemies & the four Kings | Generated, candidate-by-candidate | Fixed appearances, no paperdoll problem |
| Guild hall / feast scene | Generated | One elaborate set piece |
| Inspect portraits | Procedural today; generated portrait *frames/backdrops* later | Portrait renders the live paperdoll |
| UI chrome, panel textures | Generated, low priority | — |

## Phases

1. **Phase 0 — scaffolding (done 2026-07-21):** ART-BIBLE.md skeleton, art-refs/ drop folder, this plan, PixelLab MCP setup instructions below.
2. **Phase 1 — style lock:** owner drops reference images in `docs/art-refs/`; Claude codifies concrete rules into ART-BIBLE.md; owner picks the pending character-fidelity study (procedural side keeps moving independently).
3. **Phase 2 — first generated asset, end to end (DONE 2026-07-21):** Verdant Fields background generated via the PixelLab REST API (`/generate-image-pixflux`, 214×100 → 3× nearest upscale onto the P=3 grid at 640×300, zone palette forced via `color_image`), wired into `drawScene` behind the `BG_PLATES`/`registerBgPlate` registry with procedural fallback, judged in-game via the preview harness. Asset: `client/public/assets/zones/verdant-fields.png`; provenance (all candidates, palette image, seeds in filenames' history) in `docs/art-src/verdant-fields/`. Learnings: prompt for light from the UPPER RIGHT (engine sun at x=552); reject candidates with a baked-in sun disc (engine draws its own); the API works with the MCP token via `Authorization: Bearer` even when the MCP tools aren't loaded.
4. **Phase 3 — backgrounds volume (DONE 2026-07-21):** all four zones + the feast hall have plates. Gloomwood = glowing root tunnel; Forgotten Crypt = vaulted colonnade lit from upper right; Emberdeep = lava-river valley (rejected: a candidate with a baked-in sun, and one that read as platformer ledges — background art must never read as walkable geometry); Feast Hall = plain timber wall plate hooked into `drawFeastBack` behind the animated windows/hearth/banner (over-stripped "no furniture no windows" prompts produce black voids — ask for "wall filling the entire frame, evenly lit"). All candidates in `docs/art-src/`; judged in-game per zone via `qa-kitsune-preview.mjs <stage|feast>` + the preview harness.
5. **Phase 4 — midground prop sets (DONE 2026-07-22):** each zone has a 4-sprite set (large anchor + rock + flora + biome fauna: rabbit/owl/rat/salamander) in `assets/props/<zone>-<slot>.png`, drawn by hash-of-world-tile placement (varied choice, ±jitter, flips, 1-in-5 empty tiles, small fillers between anchors — deterministic, identical on every client) via `PROP_SPRITES`/`registerPropSprite(zone, img, slot)`; procedural props remain the fallback. Learnings: the API floor is 32×32 — generate small props at 32×32 and auto-trim transparent margins; scale per the ART-BIBLE scale rule (the first rabbit shipped at 3× and towered over the party); force fur/feather colors into the palette or fauna comes out biome-tinted (lime-green rabbit).
6. **Phase 5 — ground strips + prop grounding (DONE 2026-07-22):** seamless horizontally-tileable ground strips for all four zones (`assets/ground/<zone>.png`, 480×69) via a new recipe: pixflux 160×23 → roll half-width so the tiling seam lands center → `/inpaint` the seam band (28px white mask) → 3× upscale. `GROUND_STRIPS`/`registerGroundStrip` replaces the flat color slabs; tufts, light pools, depth grade, and tilt-shift stay on top. Props also gained base-sinking (2–6px scaled by height) + contact-shadow ellipses after the owner flagged hovering bases — trimmed bounding boxes snap the lowest pixel to the line, but irregular bases need to bed in. Note: the inpaint endpoint caps at 200px wide; the pixflux free tier is 40 generations (exhausted mid-pass, owner added credits). **Perspective learning (v0.1.12):** ground strips must be generated with `view: "low top-down"` and prompted as a foreshortened surface plane with a flat top border — "side cross-section" prompts produce an eye-level view (grass silhouetted against the background) that breaks the game's implied elevated camera. The strip's main pass draws at GROUND−20 (8px above the old slab top) so the surface lip overlaps the plate's flat bottom band, with a bottom-aligned filler pass beneath. **No flat rows at the strip top (v0.1.13):** generators tend to leave a few near-uniform rows above the actual surface texture — measure per-row variance and trim them before shipping, or the blank margin reads as a color bar at the mid/ground seam (strip heights may differ per zone after trimming). **Seam polish rules (v0.1.14):** never prompt "framing at the corners" for plates (produces flat bars — texture must reach the bottom edge); trim the strip's last row too (the seam-inpaint leaves a bright artifact line); below the strip, smear its last clean rows to the frame bottom (a second tiled pass shows a contrast seam); ground prompts must demand "rich texture filling the entire strip to every edge" (any "darkening into shadow" clause licenses flat zones); the engine draws a depth-haze feather plus a palette-matched clump fringe at the lip so the mid/ground boundary is never a ruler line. Repeated stone courses read as WALLS — paths need irregular rounded stones. Review board: `prototype/biomes.html` (all four biomes live).
7. **Phase 6 — enemies (DONE 2026-07-22):** all four zone enemies (slime, cave bat, skeleton, imp) have generated sprites in `assets/enemies/<kind>.png`, drawn via `ENEMY_SPRITES`/`registerEnemySprite(kind, img)` inside drawEnemy's scale transform — bottom-center on the ground line, with boss crown/elite marks/telegraphs/HP bars staying procedural on top, and light engine motion (slime squash-stretch, bat hover + flap lift, skeleton/imp step-bob) replacing the procedural frame animation; enraged enemies draw a cached red-tinted copy (`spr._rage`). Recipe: pixflux at ~2× texel size (48×32 small, 40–48×64 humanoid) → `fit-enemy.mjs` (trim, hard-alpha nearest resample to an exact texel height, 2× upscale, optional mirror) — slime 14 texels, bat 17, skeleton/imp 29 per the ART-BIBLE scale rule. Learnings: the PixelLab MCP tools lack `color_image`/`seed` (REST-only features) — force the palette by naming hex colors in the prompt, and beware dark prompts drifting muddy (the first skeleton came out umber until "bright bone-white ivory dominates the sprite" plus negative "dark bones, brown"); enemies must face LEFT toward the party — prompt "side view facing left" and mirror stragglers with fit-enemy's flip arg; the Kings need no separate art (same kind sprite at scale 1.8 + the procedural crown); `show_image: true` on the MCP tools breaks response marshaling — keep it false and Read the saved PNG instead. Judged per zone via the preview harness + `prototype/biomes.html` (both now register `enemy-<kind>.png`).
8. **Phase 7 — the HD canvas (character-detail direction, owner-picked 2026-07-22):** heroes move from 70px procedural paperdolls to source-native generated art (gen-2 pixel-realism grade, 128×256) on a **2× render canvas** (1280×600 device, world stays 640×300 logical — the renderer derives S from canvas width). Two-lane density is the new normal: fine action line (heroes, then enemies), chunky diorama behind (Star Renegades' own composition). Sub-phases, each shippable: **A (DONE, v0.1.20)** render-scale wrapper + facing-keyed `HERO_SPRITES` registry in both renderers, dormant; **B (DONE, v0.1.21)** 2× canvas live in client + prototype and all four enemies regenerated at HD density — `registerEnemySprite(kind, img, hd)` stamps `_ds = 2` and drawEnemy renders at half pixel size; assets in `assets/enemies/hd/<kind>.png` (classic files stay as fallback), raws in `docs/art-src/enemies-hd/`. HD-gen recipe: pixflux at final art size (slime 64², bat 96×80, humanoids 64×128) with the canon `ENEMY_COLORS` ramp as `color_image` + hexes in the prompt → trim → hard-alpha resample to exact art height (slime 56, bat 68, humanoids 116 = 2× logical) → NO texel upscale. Learnings: the zone grade mutes sprites in dark zones — judge brightness IN-ENGINE, not on the raw roll (three imp re-rolls: the brightest roll beat the best-posed roll under Emberdeep's grade); "full side profile facing left" produces better stances than "side view" alone; **the facing gate applies to every regeneration (v0.1.24):** the HD bat and skeleton shipped facing right/away and were mirrored in place after going live — exactly the classic-sprite v0.1.16 miss repeated, so judging any enemy roll must include an explicit "faces LEFT toward the party" check (zoom the sprite; muzzle, eyes, feet, and weapon side all count); **C (DONE, v0.1.22)** the kitsune as first HD hero: separately-GENERATED layers instead of slicing one image (clean separation by construction) — body at 128×256 (fitted 102×246, `heroes/kitsune-e-body.png`) + ONE tail at 128×64 (fitted 118×40) drawn as five engine instances fanned up-left behind the body, per-instance sway phase (the HD Nine-Tails). Cosmetics-driven mapping (`heroSpriteSetFor`): Kitsune Crown + Nine-Tails cape equipped = HD kitsune; registry stays name-keyed for harnesses. Portrait zooms 3× for HD (4× procedural), origin shifted right for the tail fan. Gen learnings: pixflux refuses "no tail" on a kitsune (body keeps a small baked tail — the fan hides it); multi-tail fan images come out pink-dominant/fused — generate ONE tail and instance it in-engine (also gives true per-tail sway); canvas rotate() positive sweeps a left-pointing sprite upward; **D (first wave DONE, v0.1.23 — tanks)** class base bodies + gear-overlay layers, paperdoll combinatorics preserved. Architecture: one pinned **base body** per class x body type (`tank-m`/`tank-f`, bald, canon skin, a snug dark long-sleeved BODYSUIT mannequin, loose fists, no weapon; 128×256 pixflux, fitted to artH 246/240) plus **wardrobe overlays extracted by inpaint-then-diff**: each outfit/hairstyle is inpainted onto the pinned base roll and pixel-diffed against it, so the overlay fits the body by construction; all of a body's layers ship on one shared fitted canvas and draw at a single dest rect (anchors per body in `HD_BODY_META`: centerline, sole row, fist). Hair is generated SILVER-GRAY and runtime-tinted to all nine catalog colors (two-tone c2 tips shift the style's lower reach); the warrior axe is generated once in Steel and runtime ramp-swapped to the other four skins (luminance remap of cool near-grays onto the skin's cD→c→cL→edge ramp; wood passes through). Gating is all-or-paperdoll in `heroSpriteSetFor`: every visible cosmetic needs an HD layer (hats/capes/accessories and non-warrior styles fall back whole). Tooling: `scripts/art/gen-hero-d.mjs` (body/pick/wear/weapon; REEXTRACT=1 re-runs extraction from a saved inpaint without API calls), judge scenario `make-view.mjs <stage> tanks`. Hard-won learnings: `/inpaint` caps at **200×200** — repaint tall bodies through ≤200-row windows (dressed context above each seam, then re-inpaint a 24-row band across every window seam, the ground-strip trick); **never leave "bald" in an inpaint description that asks for hair** — it beats the hair clause every time (a five-seed A/B chain traced total hair refusal to that one word); skin-to-cloth inpaints refuse sleeves (heroic-bare-arms prior) — the bodysuit mannequin makes them cloth-to-cloth and compliant, and sleeveless outfits degrade to visible suit arms by design; outfit prompts need "medieval fantasy" + anti-techwear negatives + "fully clothed" + guidance 10; negatives are not compositional — "beard, facial hair" in a hair pass suppresses ALL hair; hair masks confine to head columns, and extraction filters overlay pixels by near-neutral chroma (so a repainted face or collar can't leak into the tintable layer) plus a face guard (no hair on the lower front face; deep styles additionally keep only the behind-the-head third below the neck); windowed inpaint sometimes paints floating debris near the silhouette — drop overlay components not connected to the body. **D PAUSED / TANKS ROLLED BACK (2026-07-24, v0.1.25):** wave 2 (dps/healer bodies + bow/dagger/chainblade/staff weapons) was rolled and judged — 40 body candidates over two seed rounds, weapons settled (new learnings: pixflux refuses a METAL bow the way it refuses sleeves — generate wood and remap warm pixels onto the Steel ramp at fit time, `steelify` in gen-hero-d.mjs; REFIT=1 reprocesses a saved weapon raw with no API call) — but the owner judged the whole mannequin lane **off-style against the kitsune** (gaunt photoreal faces vs her painterly softness) and pivoted to the **character creator** (v0.1.25: six cosmetic races with procedurally drawn features, nine runtime-tinted skin tones, undergarments; `applyAppearance` + the `appearance` intent; judge scenario `make-view.mjs <stage> races`). The shipped tank layers were removed (HD_BODY_META emptied in both renderers, registrations dropped, assets deleted — raws remain in docs/art-src/heroes-d/, engine machinery dormant). When D resumes: inherit the creator's race × gender taxonomy (bodies per race, skin via runtime tint like hair), and match the kitsune's painterly look — prompt away from photorealism before spending any dressing budget. **Painterly probe run 2026-07-24 (scripts/art/gen-hero-probe.mjs, 24 rolls ≈ $0.31, raws in docs/art-src/heroes-d2/, checkpoint gallery artifact 41a91ce5-5d11-4f6f-bbeb-6b3e69c2bb0f — owner verdict pending):** (1) pixflux cannot reach the painterly bar by prompt alone — an explicit painterly clause + photoreal negatives left the mannequins in the rejected look, with back-facing strays; (2) **bitforge style transfer works** — the kitsune raw as `style_image` carries her softness/warmth/palette convincingly; (3) `style_strength` is a CONTENT-leak dial: 60 leaks hair+wardrobe, 25 gets males bald-compliant (suit tints wine) but females inherit the reference's long hair at every strength; (4) **the style image's palette IS the output palette** — a desaturated reference yields grayscale output, so any production reference must already wear the target palette; (5) API caps: bitforge maxes at 200×200 like /inpaint, and `style_image` must exactly match the output size (bodies roll 100×200 → 1.23× fit-upscale to artH 246). Proposed next step (gated on the checkpoint): the bootstrap recipe — recolor the compliant bf25-human-m-s1 roll's suit to canonical dark gray and make that bald mannequin the standing style reference, ladder against it, then the race × gender matrix. **Owner approved the mannequin-per-race×gender direction (kitsune's detail grade, not her look) and the bootstrap ran 2026-07-24 (~$0.45, checkpoint-2 gallery on the same artifact):** the recolored donor still forced crossed arms (a style reference carries POSE as well as palette — /inpaint could not repose it: unmasked shoulder stubs force raised arms, and even a shoulder-covering mask failed 6 seeds), so the standing reference is **style-ref-v2.png, built by pixel surgery** (scripts/art/build-ref-v2.mjs: crossed arms cleared, chest cloned from the waist rows, hanging arms cloned from the roll's own leg texture, skin fists) — crude up close but bitforge inherits both its style AND its arms-at-sides pose. Matrix state (b235 lane, 6 races × 2 genders × 2 seeds): human/dwarf fully compliant, elf-m excellent; **fem rolls with race-flavor words grow hair at every strength/negative** — repair proven in scripts/art/balden.mjs (head-window /inpaint opening with "completely bald" — the 7D landmine used deliberately; elf-f repair is spec-clean, but keep "completely human face" in the kitsunekin repair description or the fox-ears clause re-anthros the head); **"fox-kin" prompts full anthro** (wolf heads) — say "young man/woman with a completely human face and fox ears", and note "silver-gray fox ears" feeds silver HAIR; **the orc prior pulls gray-green mottled skin** — needs an explicit canon-warm-skin clause (skin tones are runtime tints); tiefling horns generate well, fem hair + stray cloak/blade artifacts to re-seed. Remaining before dressing (gated on checkpoint-2): per-combo seed-shop to clean pins (arms at sides, boots, no artifacts, bald via balden where needed), orc re-prompt, then in-engine scale/brightness judge. **SUPERSEDED — THE T-POSE SHEET RECIPE (owner-picked 2026-07-24, the working lane after both synthetic-donor and kitsune-surgery lanes were rejected):** generate REFERENCE SHEETS, not standing characters — `pixel art character reference sheet of one <subject> shown twice side by side, LEFT figure is the front view and RIGHT figure is the back view, both standing in a T-pose…, completely bald head, wearing only simple linen undergarments` at 256×256 pixflux (scripts/art/gen-tpose.mjs). The sheet format hits a prior the model natively knows: every roll comes out bald, dressed, and view-matched — the exact asks that standing-pose prompts fought for dozens of seeds. A flat cream card background appears despite no_background: key it by border flood-fill at fit time (enclosed linen tones survive), split front/back at the widest transparent column gap, trim, hard-alpha resample to canon height. **Canon human bases (owner picks): fem = px-human-f-s2 (base lane) fitted 240, masc = px-soft-m-s1 (soft lane) fitted 246 → tpose/base-human-{f,m}-{front,back}.png.** Race sheets reuse the same format with the per-race feature clauses (human-face guard for kitsune-kin, warm-skin clause for orc, beardless dwarf), fem on the base lane and masc on the soft lane to match the human picks. **REDIRECT (owner, 2026-07-24 evening): both synthetic donors rejected — the KITSUNE HERSELF is the donor.** Plan: strip her hair and stylized clothing at native 128×256 (step 1, DONE — `scripts/art/undress-kitsune.mjs`, result `docs/art-src/heroes-d2/kitsune-donor.png`, checkpoint-3 gallery pending verdict), then dress her in basic undergarments (step 2) and use THAT as the standing style reference for the race × gender mannequins. Step-1 method learnings: **the inpaint mask SHAPE is the prior** — a hair-shaped repaint region regrows hair (recolored skin-tan) even over pre-cleared pixels; true geometry removal = clear the pixels, leave them UNMASKED so they stay transparent, and repaint only a thin closure band (2-3px dilation of the surviving silhouette); **clothing is the opposite case** — it wraps the body, so shape-preserving masked repaint (no clearing) turns the dress-shaped region into skin; **an anime skull is mostly hair volume** — after clearing, force a cranium-dome repaint region (ellipse anchored on the surviving face) or the bald head comes out pin-sized; largest-component debris cleanup can delete the HEAD when hair-clearing severs the face from the neck — use a ≥50px size threshold instead; rigid geometry (the leaning spear) is best restored by stamping the raw's own pixels back deterministically, with the dark rule bounded to the shaft's drift band or the cleared tail's outline ghosts back. Old next-wave notes kept for that day: remaining outfits/hats/capes per body, paladin needs a shield layer decision; **E** portraits/inspect from the same art; **F** south facing for the feast (north later, if a scene ever shows it) — the PROCEDURAL half of F shipped in v0.1.26: drawFeaster now draws a true south face (centered features; drawRaceFace/drawAccessory take a `south` flag for race features and face marks), so the feast no longer reuses the east ¾ paperdoll head; the HD half (generated south layer sets) still waits on the re-rolled painterly bodies; **G — enemy puppet pass (after C proves the rig):** re-slice the HD enemy sprites into 2–3 layers and drive them with the hero puppet machinery — bat wings and the two humanoids' weapon arms first, the slime last or never (its squash-stretch already carries it). No new generation: the layers are cut from the shipped HD art, and the gap this closes only appears once puppet-animated heroes stand next to single-layer enemies. Phase-0 spike findings (branch `hd-canvas-spike`): 2× draw cost only 1.3× (0.74 ms/frame); PixelLab caps — `rotate` ≤200px (moot for combat: generate east-facing natively; the game is side-view), `animate-with-text` ≤64px (unusable for HD), `animate-with-skeleton` square canvases {16..256} only, exactly 3 keypoint frames per call, re-renders at 256 with identity intact but needs authored pose libraries (crude keypoint edits smear limbs) — hence engine-puppet animation is primary. MCP tool errors at these sizes are opaque (`[object Object]`) — use the REST API from scripts for anything above default sizes. Older Phase-7 targets (inspect-portrait backdrops, feast-hall floor strip) fold into E/F. **THE CAST IS FITTED (owner picks, 2026-07-24 late): E-F1, E-M1, K-F2, K-M1, D-F2, D-M3, O-F3, O-M1, T-F2, T-M1 → `tpose/base-<race>-{f,m}-{front,back}.png`, twelve bodies incl. the human pair, lined up on `tpose/cast-{front,back}.png`.** Heights come from the creator's own taxonomy rather than taste: every race is 240 fem / 246 masc except the dwarf, whose `RACES` entry carries `build: 0.86` → **206 / 212** (`buildW: 1.1` is NOT baked — the roll's own dwarf proportions carry width, and the engine's widen transform is a procedural-paperdoll device). **Note the sheets are not actually T-poses** — the prompt asks for one, but every roll comes back arms-at-sides, front and back, which is what the paperdoll wants; keep the prompt (the phrase is what summons the reference-sheet prior) and stop describing the output as T-posed. **Six of the ten picks needed feature repair, and all of it was authored, not re-rolled** (a re-roll is a different character, and the owner had already spent three chooser rounds): `scripts/art/fix-picks.mjs` holds the ops per pick, with `probe.mjs` / `where.mjs` / `zoom.mjs` (colour-index grid, connected-cluster locator, gridded nearest-neighbour zoom) as the instruments that make pixel-exact surgery tractable. **The hard-won rule of this pass: the garments are painted in the card background's own hex (#ece2cd), so nothing structural can be decided by colour** — every edit that touches a silhouette must first flood-fill the background in from the border and ask "is this pixel OUTSIDE", the same test the fit step uses. Three separate bugs came from ignoring it: a seam-strip pass that dissolved the hip silhouette, a `rim` pass that drew no rim because cloth-cream sits against card-cream, and an /inpaint mask that swallowed the arm-torso gaps so the model filled them solid white. Other learnings: **erasing a defect can cost the shading that sold the form** — deleting a dwarf's gray skull wedge left a flat white cap on the cast sheet, and the fix was to recolour it to a warm skin shadow instead; **healing (nearest-neighbour fill) is wrong on a crown**, where the nearest neighbour is the outline and it floods the skull brown — recolour to the sheet's dominant skin tone, then clear leftover strands with an interior-only pass so the outline survives; **a garment band must follow the torso**, walked out from the spine per row, or it lands as a pasted rectangle. Race features now match engine canon: orc tusks were pale streaks running down the cheeks (and pendants hanging off the jaw on the fem roll) and were replanted as the `drawRaceFace` pip — ivory `#efe6cf` over `#c9bfa4`, jutting UP from the lower jaw flanking the mouth — after clearing the roll's pale mouth band, which otherwise fuses with the new tusks into one row of teeth. **Bases must be bald**, since hairstyles are runtime-tinted overlays: two picks carried a baked gray crop and were balded by palette swap, no API call and no face drift. **/inpaint lost again on cloth** (4 seeds, $0.038): asked to turn a brown tube top and denim-styled shorts into linen underwear over a correctly-masked chest+hip window, it returned shapeless bands and smeared cloth worse than the crisp roll — the fix that shipped is a recolour to the sheet's linen cream plus a rimmed band wrapped around the back view (the roll drew the band on the front only, impossible once the two halves are one character's layers). Running total for the T-pose lane's repairs: **$0.038**.

## Engine integration rules

- **Asset loader with procedural fallback.** `render.js` gains an image cache; every generated asset has the existing procedural drawing as its fallback when the image is missing/unloaded. The game must never break because an asset didn't ship.
- **The standalone-build wrinkle:** the prototype ships as ONE self-contained HTML file (claude.ai artifact + GitHub Pages). Generated assets must be **inlined as base64 data URIs** by `scripts/build-standalone.js` (or the prototype simply keeps procedural fallbacks — decide at Phase 2 based on file-size impact; the artifact has practical size limits).
- **Grid discipline:** generated images are authored at native resolution (e.g. a full bg layer at 640×300, props on the P=3 grid), *not* upscaled art. Post-process every generation: snap to grid, quantize to the zone ramp, kill AA edges. Retro Diffusion's [pixel-art-fixer](https://github.com/Retro-Diffusion/pixel-art-fixer) or a small script can do this.
- **The cardinal rule still applies:** any drawing-code change (including the asset-loader hooks) lands identically in `client/src/render.js` and `prototype/guild-idle.jsx`.
- Background layers get the engine's depth blur/tilt-shift/grade passes — author for post-processing, don't bake blur in.

## PixelLab MCP setup (owner action required)

1. Sign up at https://www.pixellab.ai and get an API token (interactive setup guide: https://www.pixellab.ai/vibe-coding).
2. Add the MCP server to Claude Code (run from the project you'll do art sessions in):

   ```
   claude mcp add pixellab -e PIXELLAB_SECRET=YOUR_TOKEN -- npx -y pixellab-mcp
   ```

   (Exact invocation per https://github.com/pixellab-code/pixellab-mcp — check its README if the package name changed.)
3. Tools exposed: `create_character`, `animate_character`, `create_tileset`, `create_isometric_tile`. For this project the tileset/tile tools and (for enemies) character generation are the relevant ones — party characters stay procedural.
4. Secondary option for backgrounds/stills: Retro Diffusion (https://retrodiffusion.ai/) has an API; script it if PixelLab's tileset orientation doesn't fit full-scene backgrounds.

## Visual iteration loop

- Art sessions require **seeing** the result: run the prototype (`npx serve prototype` or the standalone HTML) in the Claude browser pane. The pane must be **visible** — when hidden, the browser suspends requestAnimationFrame and the canvas stays blank.
- Claude captures pixels via screenshot or `canvas.toDataURL()` → PNG → visual inspection, then iterates against the ART-BIBLE.md checklist.
- Optional improvement: a headless Node renderer (node-canvas + DOM shim; `ctx.filter` blur must be shimmed to no-op) so soak scenarios export PNGs without a browser. Not required for the pipeline to work.
