node_modules/
client/dist/
server/guild.db*
server/world.json*
server/.env
*.log

# Copyrighted reference screenshots — private dev references only, never commit.
# (README.md in that folder stays tracked.)
docs/art-refs/*
!docs/art-refs/README.md

# Cosmetic-preview dev harness intermediates (regenerated by qa-kitsune-preview.mjs
# / qa-pet-preview.mjs)
prototype/kitsune-view.json
prototype/feast-pets-view.json
prototype/render.bundle.mjs
prototype/zone-*.png
prototype/prop-*.png
prototype/ground-*.png
prototype/enemy-*.png
prototype/kitsune-view-*.json

# Local Claude session tooling (dev-preview launch config)
.claude/

# Review GIFs assembled from harness captures (regenerable, heavy)
docs/art-src/enemies/gifs/
